============
Installation
============

At the command line::

    $ easy_install invenio-previewer-demobbed

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv invenio-previewer-demobbed
    $ pip install invenio-previewer-demobbed